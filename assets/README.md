
NOTE: Firebase config files were removed (they belonged to the worker app,
com.cguardpro.operaciones). Add THIS app's own GoogleService-Info.plist and
google-services.json (registered for com.cguardpro.supervisor) into ios/App/App/
and android/app/ after `npx cap add ios android`.
